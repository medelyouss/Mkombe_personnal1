{% extends 'admin/base.html.twig' %}

{% block title %}ProductColor{% endblock %}

{% block body %}
    <h1>ProductColor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_color.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ product_color.designation }}</td>
            </tr>
            <tr>
                <th>CodeColor</th>
                <td>{{ product_color.codeColor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_color_index') }}">back to list</a>

    <a href="{{ path('app_color_edit', {'id': product_color.id}) }}">edit</a>

    {{ include('admin/product_color/_delete_form.html.twig') }}
{% endblock %}
